---
- name: Render a template
  hosts: all
  become: true
  vars:
    listen_port: 8080
  tasks:
    - ansible.builtin.template:
        src: app.conf.j2
        dest: /etc/devtrack.conf
        mode: '0644'
